"""
8.序列：
  有序，可以存放一组数据的容器
  `list`、`tuple`、`range()`、字符串

共同点：
  1. 有序排列，下标都是从 0 开始
  2. 支持下标（索引）取值，正反取值
  3. 支持切片取值
  4. 支持 `for` 循环输出
  5. 都可用 len() 进行取值

"""

name = "hello"

for i in name:
    print(i)
